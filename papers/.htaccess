# NOTE 2026-08-10: a 301 for inward-migration-economics.pdf lived here from
# 2026-07-11 (when that filename was believed retired) until it was found
# hijacking Paper D's canonical PDF URL. The filename is live again; do not
# re-add a redirect for it. (OCS-D-PDF-REDIRECT-1)

# DreamHost's Apache runs .py as a CGI script by default, so the published
# figure-generation source mirror (papers/figs/*.py) returned 500 Internal
# Server Error instead of serving. These files are reproducibility SOURCE, not
# executables — serve them as readable plain text and strip any CGI handler.
RemoveHandler .py
AddType text/plain .py
<FilesMatch "\.py$">
  SetHandler none
  ForceType text/plain
</FilesMatch>
